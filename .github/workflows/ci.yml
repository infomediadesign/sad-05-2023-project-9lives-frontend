name: DEPLOY TO DOCKERHUB FOR FRONTEND

on:
  push:
    branches: ["ci_kp"]
  pull_request:
    branches: ["ci_kp"]

env:
  DOCKER_USERE: ${{ secrets.DOCKER_USER }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
  DOCKER_REPO: kishor97p/sad_05_2023_project_9lives

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build and Tag Docker Image
        run: |-
          docker build -t ninelives-frontend:1.0 .

      - name: Docker Login
        env:
          DOCKER_USER: ${{ secrets.DOCKER_USER }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: |
          docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

      - name: Rename the tag
        run: docker tag ninelives-frontend:1.0 kishor97p/sad_05_2023_project_9lives:ninelives-frontend

      - name: Push to Dockerhub
        run: docker push kishor97p/sad_05_2023_project_9lives:ninelives-frontend
